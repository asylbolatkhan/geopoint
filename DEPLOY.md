# GeoVictorina — деплой нұсқаулығы (мұғалімге арналған)

> ## ✅ Деплой аяқталған (2026-07-23)
>
> Бұл нұсқаулық орындалып қойды — жүйе жұмыс істеп тұр:
>
> - **Бот:** [@Capitto_bot](https://t.me/Capitto_bot) — мәзір батырмасы, сипаттамасы бапталған
> - **Mini App / API:** https://geo-server-production.up.railway.app
> - **Railway жобасы:** `geovictorina` (сервистер: `geo-server` + `Postgres`); ескі сайт жобасына тиіспеген
> - Дерекқор миграциясы қолданылған, мектеп жазбасы себілген (`ГеоВикторина мектебі`)
> - Деплой әдісі: GitHub емес, тікелей жүктеу (`railway up`). **Код өзгергенде қайта деплой:**
>   `railway up --service geo-server` (жоба папкасынан)
>
> Төмендегі мәтін — анықтама ретінде сақталған түпнұсқа нұсқаулық.

Бұл нұсқаулық бағдарламашы болмасаңыз да қадам сайын орындай алатындай етіп жазылған. Керек болатын нәрселер: Telegram аккаунты, [Railway](https://railway.app) аккаунты (GitHub арқылы кіруге болады) және осы жоба орналасқан GitHub репозиторийіне қатынас.

Жүйе үш бөліктен тұрады:
- **web (түбір)** — қолданыстағы сайт (geopoint-website), Railway-де бөлек қызмет ретінде **бұрыннан** істеп тұр. Оны **өзгертпейміз**.
- **tma/** — Telegram Mini App (оқушылар ашатын ойын беті).
- **server/** — API + Telegram бот + дерекқормен жұмыс. `tma`-ны құрастырып, өзімен бірге серверлейді.

Төменде **тек жаңа қызметті** (bot + Mini App + API) қалай іске қосу керегі жазылған.

---

## 1-бөлім. BotFather арқылы бот жасау

1. Telegram-да [@BotFather](https://t.me/BotFather) ботын ашыңыз.
2. `/newbot` командасын жіберіңіз.
3. Ботқа атау беріңіз (мысалы, «GeoVictorina»).
4. Ботқа username беріңіз — ол `bot` деген сөзбен аяқталуы керек (мысалы, `geovictorina_bot`).
5. BotFather сізге **токен** береді — ұзын әріп-сан тізбегі (мысалы, `123456:ABC-DEF...`). Бұл токенді көшіріп, қауіпсіз жерде сақтаңыз (кейін `BOT_TOKEN` айнымалысына керек болады). **Ешкіммен бөліспеңіз.**

> Ботты Mini App-қа қосу қадамын 3-бөлімнің соңында («WEBAPP_URL қою») жасаймыз, себебі ол үшін алдымен серверді деплой етіп, доменін білу керек.

---

## 2-бөлім. Railway — жаңа қызмет жасау

1. [railway.app](https://railway.app) сайтына кіріңіз (GitHub аккаунтыңызбен).
2. Жобаңызды ашыңыз (geopoint репозиторийі байланыстырылған жоба).
3. **New** → **GitHub Repo** батырмасын басып, **сол дәл GeoVictorina/geopoint репозиторийін** таңдап, жаңа қызмет (service) қосыңыз.
   - Назар аударыңыз: бұл — қолданыстағы `geopoint-website` қызметінен **бөлек, жаңа** қызмет. Ескі қызметтің Build/Start баптауларын **өзгертпеңіз** — ол өз күйінде қалуы керек.
4. Жаңа қызметтің атын өзгертіңіз, мысалы `geovictorina-app`.
5. Осы жаңа қызметтің **Settings** бетіне өтіп, мына мәндерді дәл көшіріп қойыңыз:

   **Build Command:**
   ```
   npm ci --prefix tma && npm run build --prefix tma && npm ci --prefix server
   ```

   **Start Command:**
   ```
   npm run start:prod --prefix server
   ```

6. Дерекқор қосу: жобаңызда **New** → **Database** → **Add PostgreSQL** басыңыз. Railway автоматты түрде PostgreSQL қызметін жасайды.

7. Енді жаңа қызметтің **Variables** бетіне өтіп, төмендегі айнымалыларды қосыңыз:

   | Айнымалы | Түсіндірме |
   |---|---|
   | `DATABASE_URL` | Дерекқорға қосылу жолы. Мәнін қолмен жазбаңыз — Railway-де «Reference Variable» ретінде PostgreSQL қызметінің `DATABASE_URL`-ін таңдаңыз (Variables → New Variable → «Add Reference» → Postgres → `DATABASE_URL`). |
   | `BOT_TOKEN` | 1-бөлімде BotFather берген токен. |
   | `ADMIN_TG_ID` | Сіздің (мұғалімнің) Telegram сандық ID-іңіз. Оны білу үшін Telegram-да [@userinfobot](https://t.me/userinfobot) ботын ашып, `/start` басыңыз — ол сізге ID нөміріңізді жібереді. |
   | `WEBAPP_URL` | Осы жаңа қызметтің жария (public) домені, мысалы `https://geovictorina-app-production.up.railway.app`. Бұл мән тек **бірінші деплойдан кейін** белгілі болады (төмендегі 3-бөлімді қараңыз) — сондықтан алдымен бос қалдырып, кейін қосып, қайта деплой етесіз. |
   | `SCHOOL_NAME` | Мектебіңіздің атауы, мысалы `№15 мектеп-гимназиясы`. |
   | `SEED_CLASSES` | Бастапқыда жасалатын сыныптар тізімі, үтірмен бөліп жазыңыз, мысалы: `7А,7Ә,8А`. |
   | `NODE_ENV` | `production` деп қойыңыз. |

   > **Маңызды:** `DEV_AUTH` айнымалысын production-да **қоспаңыз** (мүлде жазбаңыз). Ол тек компьютерде әзірлеу кезінде ғана қолданылады, ал production-да қосылып қалса, кез келген адам өзін кез келген оқушы етіп көрсетіп кіре алады — бұл қауіпсіздікке қауіп төндіреді.

8. Айнымалыларды сақтағаннан кейін Railway автоматты түрде деплой бастайды (немесе **Deploy** батырмасын өзіңіз басыңыз).

---

## 3-бөлім. Алғашқы іске қосу

1. **Deploy Logs** бетін ашып, деплойдың сәтті аяқталғанын бақылаңыз. Логтарда мына жол болуы керек:
   ```
   migrations up to date
   ```
   Бұл дерекқор кестелерінің дұрыс жасалғанын білдіреді.

2. **Бір реттік seed** (сыныптар мен мектеп жазбасын жасау). Railway дашбордында интерактивті shell жоқ, сондықтан төмендегі екі тәсілдің бірін қолданыңыз:

   **А тәсілі — Railway CLI арқылы (ұсынылады):**
   - Өз компьютеріңізге Railway CLI орнатыңыз ([railway.app/cli](https://railway.app/cli) нұсқаулығы бойынша), содан кейін кіріңіз:
     ```
     railway login
     ```
   - Жоба түбірінде (репозиторийдің басты қалтасында) қызметті байланыстырыңыз:
     ```
     railway link
     ```
     Тізімнен жобаңызды, орталарды (environment) және жаңа қызметті (мысалы `geovictorina-app`) таңдаңыз.
   - Содан кейін seed команданы сол қызметтің орта айнымалыларымен (Variables) іске қосыңыз:
     ```
     railway run npm run seed --prefix server
     ```
   - «created school ...» және сыныптар атаулары шыққанын көресіз.

   **Ә тәсілі — өз компьютеріңізден, `DATABASE_URL`-ді қолмен беріп:**
   - PostgreSQL қызметінің `DATABASE_URL`-ін (Variables бетінен) көшіріп алыңыз.
   - Терминалда (`server` қалтасында тұрып емес, жоба түбірінде):
     - **Git Bash / macOS / Linux** үшін (айнымалыны команданың алдына жазу тәсілі):
       ```
       DATABASE_URL="<көшірген_мән>" SEED_CLASSES="7А,7Ә,8А" SCHOOL_NAME="№15 мектеп-гимназиясы" npm run seed --prefix server
       ```
     - **Windows PowerShell** үшін (жоғарыдағы синтаксис жұмыс істемейді — айнымалыларды алдымен бөлек орнату керек):
       ```
       $env:DATABASE_URL="<көшірген_мән>"; $env:SEED_CLASSES="7А,7Ә,8А"; $env:SCHOOL_NAME="№15 мектеп-гимназиясы"; npm run seed --prefix server
       ```

   > Seed команданы **тек бір рет** орындау керек. Қайта орындасаңыз да қатесіз өтеді (бар сыныптар қайта жасалмайды), бірақ әдетте бұл — бастапқы баптаудың соңғы қадамы.

3. **WEBAPP_URL қою:** Егер жаңа қызметте әлі жария домен болмаса, алдымен **Settings → Networking** бетінде **Generate Domain** батырмасын басыңыз — Railway автоматты түрде домен жасайды (мысалы `https://geovictorina-app-production.up.railway.app`). Осы толық сілтемені көшіріп алып, **Variables** бетіндегі `WEBAPP_URL` мәніне қойыңыз, сосын **Redeploy** басыңыз.

4. **Ботты Mini App-қа қосу** (BotFather-де):
   - [@BotFather](https://t.me/BotFather) ашыңыз, `/mybots` → ботыңызды таңдаңыз.
   - **Bot Settings** → **Menu Button** (немесе **Configure Mini App**) бөлімін ашыңыз.
   - Батырма мәтінін жазыңыз (мысалы, «Ойынды бастау») және сілтем ретінде `WEBAPP_URL` мәнін (сол Railway домені) қойыңыз.
   - Осыдан кейін оқушылар ботты ашқанда, төменгі мәзір батырмасы арқылы тікелей Mini App-ты аша алады.

   > Бот тіркеу, растау, батл шақыруы сияқты оқиғаларда оқушылар мен админге хабарламаны **автоматты түрде өзі жібереді** — бөлек баптаудың қажеті жоқ.

---

## 4-бөлім. Тексеру чек-парағы

Барлығы дұрыс жұмыс істейтінін тексеру үшін мына қадамдарды кезекпен орындаңыз:

- [ ] Telegram-да ботты ашып, `/start` басыңыз — сәлемдесу хабары және «ашу» батырмасы шығуы керек.
- [ ] Mini App-ты ашып, өзіңізді **әкімші (admin)** ретінде көріңіз (себебі сіздің Telegram ID-іңіз `ADMIN_TG_ID`-ге тең).
- [ ] Админ панелінен жаңа сынып қосыңыз (немесе алдын ала seed арқылы жасалған сыныптардың бар екенін тексеріңіз).
- [ ] Басқа Telegram аккаунттан (немесе әріптесіңіздің көмегімен) ботты ашып, **тест-оқушы ретінде тіркеліңіз** (аты-жөні, сынып таңдау).
- [ ] Админ панелінен сол тест-оқушыны **растаңыз (approve)**.
- [ ] Тест-оқушы аккаунтынан **жаттығу (соло) режимін** ойнап көріңіз — сұрақтар шығып, ұпай қосылуы керек.
- [ ] Екі оқушы аккаунтының арасында **батл** жіберіп, қабылдап, ойнап көріңіз.
- [ ] **Рейтинг** бетін ашып, ұпайлардың дұрыс көрінетінін тексеріңіз (сынып/мектеп бойынша).

Осы қадамдардың барлығы қатесіз өтсе — жүйе толық жұмыс істеп тұр деген сөз.

---

## 5-бөлім. Ақаулықтарды жою

| Мәселе | Ықтимал себебі және шешімі |
|---|---|
| **Бот үнсіз** — `/start` жазсаңыз да жауап бермейді | `BOT_TOKEN` айнымалысын тексеріңіз: дұрыс көшірілгенін, бос емесін тексеріңіз. Deploy Logs-та `BOT_TOKEN not set — bot disabled` деген жол болса — токен жоқ немесе дұрыс сақталмаған. Variables-ты түзетіп, қайта деплой етіңіз. |
| **401 қатесі** (Mini App ашылмайды / «unauthorized») | Mini App-ты браузерден тікелей емес, **тек Telegram ботының ішінен** (Menu Button арқылы) ашу керек. Telegram сыртынан ашылса, авторизация деректері (initData) болмайды. |
| **Дерекқор қатесі** (DB error, 500) | `DATABASE_URL` айнымалысын тексеріңіз — ол PostgreSQL қызметінің Reference Variable-і ретінде дұрыс қосылғанына көз жеткізіңіз. Deploy Logs-та қосылу қатесі бар-жоғын қараңыз. |
| **Қолданба жүктелмейді** (ақ бет немесе «Application failed to respond») | Deploy Logs-ты ашып, build қадамында қате бар-жоғын тексеріңіз (мысалы, `npm ci` сәтсіз аяқталуы мүмкін). Build Command мен Start Command 2-бөлімдегідей дәл жазылғанына көз жеткізіңіз. |
| **WEBAPP_URL дұрыс емес** (батырма басқа жерге апарады немесе ештеңе болмайды) | Variables-тегі `WEBAPP_URL` мәнінің Settings → Networking бетіндегі домен мәнімен дәл сәйкес келетінін, `https://`-мен басталатынын тексеріңіз, содан кейін қайта деплой етіңіз. |
| **Seed қайта жасау керек болса** | `npm run seed --prefix server` командасын қайта орындауға болады — бар сыныптар қайта жасалмайды, тек жоқтары қосылады. |

---

## Қосымша материалдар

- Жоба құрылымы туралы жалпы ақпарат: [README.md](./README.md)
- Серверді жергілікті (локал) әзірлеу нұсқаулығы: [server/README.md](./server/README.md)
